{
  "name": "return-rate-calc-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for return-rate-calc",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "18" },
  "dependencies": {
    "firebase-functions": "^4.5.0",
    "node-fetch": "^2.7.0"
  }
}
